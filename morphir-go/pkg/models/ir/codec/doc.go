// Package codec contains serialization codecs for Morphir IR.
//
// The stable IR domain model lives in the parent package (stable/ir).
// Codecs are separated to support multiple encodings (e.g. JSON, YAML)
// and multiple format versions.
package codec
