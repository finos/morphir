package build 
import mill._, kotlinlib._ 
import $file.dependencies.V

object `morphir-cli` extends KotlinModule {
    def kotlinVersion = V.Kotlin.kotlinVersion
    def mainClass = Some("org.finos.morphir.cli.Main")

    def ivyDeps = Agg(
        ivy"com.github.ajalt.clikt:clikt-jvm:${V.Deps.clikt}",
        ivy"com.github.ajalt.clikt:clikt-markdown-jvm:${V.Deps.clikt}",
    )
}

object apps extends Module {
}