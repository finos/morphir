#!/usr/bin/env bash
# Description: Run morphir-dev CLI
# Usage: mise run run-dev

set -euo pipefail

SCRIPT_DIR="$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)"
REPO_ROOT="$(cd \"/../..\" && pwd)"

"/scripts/run-dev.sh" "$@"
