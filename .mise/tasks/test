#!/usr/bin/env bash
# Description: Run all Go tests
# Usage: mise run test

set -euo pipefail

SCRIPT_DIR="$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)"
REPO_ROOT="$(cd \"/../..\" && pwd)"

"/scripts/test.sh" "$@"
