#!/usr/bin/env bash
#MISE description="Format Go code"
#MISE usage="mise run fmt"

set -euo pipefail

SCRIPT_DIR="$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)"
REPO_ROOT="$(cd \"/../..\" && pwd)"

"/scripts/fmt.sh" "$@"
