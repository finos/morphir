#!/usr/bin/env bash
# Description: Validate GoReleaser configuration
# Usage: mise run goreleaser-check

set -euo pipefail

SCRIPT_DIR="$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)"
REPO_ROOT="$(cd \"/../..\" && pwd)"

"/scripts/goreleaser-check.sh" "$@"
