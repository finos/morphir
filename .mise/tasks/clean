#!/usr/bin/env bash
# Description: Clean build artifacts
# Usage: mise run clean

set -euo pipefail

SCRIPT_DIR="$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)"
REPO_ROOT="$(cd \"/../..\" && pwd)"

"/scripts/clean.sh" "$@"
