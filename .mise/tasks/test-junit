#!/usr/bin/env bash
#MISE description="Run tests with JUnit XML output"
#MISE usage="mise run test-junit"

set -euo pipefail

SCRIPT_DIR="$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)"
REPO_ROOT="$(cd \"/../..\" && pwd)"

"/scripts/test-junit.sh" "$@"
