#!/usr/bin/env bash
#MISE description="Run golangci-lint"
#MISE usage="mise run lint"

set -euo pipefail

SCRIPT_DIR="$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)"
REPO_ROOT="$(cd \"/../..\" && pwd)"

"/scripts/lint.sh" "$@"
