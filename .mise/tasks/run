#!/usr/bin/env bash
# Description: Run morphir CLI
# Usage: mise run run

set -euo pipefail

SCRIPT_DIR="$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)"
REPO_ROOT="$(cd \"/../..\" && pwd)"

"/scripts/run.sh" "$@"
