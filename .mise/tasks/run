#!/usr/bin/env bash
#MISE description="Run morphir CLI"
#MISE usage="mise run run"

set -euo pipefail

SCRIPT_DIR="$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)"
REPO_ROOT="$(cd \"/../..\" && pwd)"

"/scripts/run.sh" "$@"
