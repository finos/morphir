#!/usr/bin/env bash
# Description: Download Go module dependencies
# Usage: mise run deps

set -euo pipefail

SCRIPT_DIR="$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)"
REPO_ROOT="$(cd \"/../..\" && pwd)"

"/scripts/deps.sh" "$@"
