package org.finos.morphir.ir.gen1

import org.finos.morphir.testing.MorphirSpecDefault
import zio.test.*

object QualifiedModuleNameSpec extends MorphirSpecDefault {
  def spec = suite("QualifiedModuleName Spec")(
//    test("fromString") {
//      assertTrue(QualifiedModuleName.fromString("Basics") == QualifiedModuleName.unsafeMake()("basics"))
//    }
  )
}
