
workspace.projects = [
    "common",
    "project-01",
    "project-02"
]

elm {
    
    dependencies {       
        # Default - all the defaults no removal allowed
        # Managed - all the defaults with removals and custom object syntax
        # custom - you are in control no defaults
        mode = default | managed | custom
        direct {            
            "elm/json" = "1.1.3"
            "elm/core" = "1.0.5"
            "elm/html" = "1.0.0"
        }
        indirect {            
        }
    }
}