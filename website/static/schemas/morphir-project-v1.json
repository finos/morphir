{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://morphir.finos.org/schemas/morphir-project-v1.json",
  "title": "Morphir Project Configuration (morphir.json)",
  "description": "JSON Schema for the morphir.json project configuration file format (as used by morphir-elm).",
  "type": "object",
  "required": [ "name", "sourceDirectory", "exposedModules" ],
  "properties": {
    "name": {
      "description": "Package name / module prefix (Elm module style, e.g. My.Package).",
      "type": "string"
    },
    "sourceDirectory": {
      "description": "Directory containing project source files (relative to project root).",
      "type": "string"
    },
    "exposedModules": {
      "description": "Modules exposed by the package (without the common package prefix).",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "dependencies": {
      "description": "Dependency references. Implemented in morphir-elm CLI2: data: URLs, file: URLs, http/https/ftp URLs, or plain local file paths. Reserved (not yet implemented): git:, github:, npm:.",
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "localDependencies": {
      "description": "Local dependency references (backwards compatibility). In morphir-elm CLI2 these are loaded similarly to dependencies for plain paths.",
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "decorations": {
      "description": "Map of decoration id to decoration configuration.",
      "type": "object",
      "additionalProperties": {
        "$ref": "#/definitions/decoration"
      }
    }
  },
  "additionalProperties": true,
  "definitions": {
    "decoration": {
      "type": "object",
      "properties": {
        "displayName": {
          "type": "string"
        },
        "ir": {
          "type": "string"
        },
        "entryPoint": {
          "type": "string"
        },
        "storageLocation": {
          "type": "string"
        }
      },
      "additionalProperties": true
    }
  }
}
