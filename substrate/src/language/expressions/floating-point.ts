/**
 * Floating-point type — corresponds to `specs/language/expressions/floating-point.md`.
 *
 * A Number instance with binary floating-point semantics.
 * Operations are inherited from Number; this module declares metadata only.
 */

export const modulePath = "expressions/floating-point.md";

/** Floating-point format attribute. */
export type FloatingPointFormat = "binary32" | "binary64";
