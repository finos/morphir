# Configuration with deeply nested structures

[codegen]
targets = ["go", "scala", "typescript"]

[codegen.go]
package = "morphir"
module = "github.com/example/morphir"

[codegen.go.options]
use_pointers = true
generate_tests = true

[codegen.scala]
package = "com.example.morphir"
version = "2.13"
