// Package schema embeds Morphir IR JSON schema files and helper functions.
//
// Use this package to load versioned schemas (v1, v2, v3) and validate external
// payloads before decoding them into IR types.
package schema
